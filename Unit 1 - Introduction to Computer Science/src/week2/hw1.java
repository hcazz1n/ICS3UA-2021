package week2;
/**Write a program that takes a decimal number then displays the 
*number and the square to the console in a presentable way.
*/
public class hw1 {
    public static void main(String[] args) {
        double number = 7.3;
        double numSquared = number*number;
        System.out.println(number + " x " + number + " = " + numSquared);
    }
}